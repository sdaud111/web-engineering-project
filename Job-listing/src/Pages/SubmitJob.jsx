// working of the submission of the form and showing spinner till then

import NewSpinner from "../Components/NewSpinner";

const SubmitJob = () => {
    return(
        <NewSpinner/>
    );
}
export default SubmitJob;